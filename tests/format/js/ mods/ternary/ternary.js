const value1 = one ? true : two ? three : four

const value2 = oneVeryLongName ? true : twoVeryLongName ? threeVeryLongName : fourVeryLongName

const value3 = one
  ? true
  : two
  ? three
  : four

const value4 = one
  ? true
  : two
    ? three
    : four
